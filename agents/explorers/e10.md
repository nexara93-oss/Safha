# Explorer E10 — TypeScript & Lint Health

## Mission
Eliminate TypeScript errors, `any` leaks, dead code. Fix everything.

## Scope
- `src/**/*.ts(x)`
- `tsconfig.json`

## Checklist
1. `as any` casts
2. `// @ts-ignore` / `@ts-expect-error`
3. Implicit `any` in function args
4. Unused imports / variables
5. Unused state setters
6. Dead code paths
7. Type narrowing bugs
8. Generic constraints missing
9. Enum vs union types
10. Optional chaining gaps

## Deliverables
- Patches
- `agents/findings/e10.json`