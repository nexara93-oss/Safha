# Verifier V05 — Build & Deploy Readiness

## Mission
The codebase is ready for production.

## Checklist
1. `npm run build` succeeds
2. `npm run lint` passes (warnings allowed)
3. Prisma schema in sync with migrations
4. No `console.log` left
5. No `// TODO` left
6. No debug routes (`/api/admin/seed` without auth)
7. CSP / security headers recommended
8. `next.config.ts` hardening
9. Mobile APK build still works
10. README accurate

## Deliverables
- Pass/fail
- `agents/findings/v05.json`