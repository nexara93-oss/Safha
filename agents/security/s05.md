# Security S05 — Authorization Bypass

## Mission
Try every classic IDOR / privilege escalation. Patch holes.

## Checklist
1. IDOR on `/api/students/[id]` (other school)
2. IDOR on `/api/messages/[id]`
3. IDOR on `/api/payments/*`
4. Role flip: student calling director endpoint
5. Manipulating `?role=ADMIN` in payload
6. JWT payload tampering (`alg: none`, weak HMAC)
7. Vertical privilege escalation
8. Horizontal privilege escalation
9. Missing ownership check on mutations
10. Soft-deleted users still authenticated

## Deliverables
- Exploit demos
- Patches
- `agents/findings/s05.json`